//对数组名的理解
#include <stdio.h>

int main()
{
    int arr[] = {1,2,3,4,5,6,7};
    printf("%p\n",arr);           //1
    printf("%p\n",arr+1);

    printf("%p\n",&arr[0]);       //2
    printf("%p\n",&arr[0]+1);

    printf("%p\n",&arr);          //3
    printf("%p\n",&arr+1);
    return 0;
}

//1.所有的数组名都表示数组首元素的地址，两种情况除外
//  1）sizeof(数组名)，计算整个数组的大小
//  2）&数组名，取出的是数组的地址，取出的是整个数组

//所以第一种情况跟第二种情况的输出是完全一致的，两种表达方法的输出一样
//第三种情况输出整个数组的地址，+1的话 结果相差28
