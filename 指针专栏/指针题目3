///指针题目三
#include <stdio.h>

int main()
{
   int a[4] = {1,2,3,4};
   int *ptr1 = (int*)(&a+1);        ///&a就是整个数组的地址 &a+1就是跳过整个数组 指针指到4的后面
   int *ptr2 = (int*)((int)a+1);    ///(int)a 就是将数组的首元素地址强制转换成整型 +1就相当于整数+1 即地址只向后偏移了一个字节
                                    ///小端存储布局 01 00 00 00|02 00 00 00|03 00 00 00|04 00 00 00
                            ///向后偏移一个字节之后 因为是int*类型 所以取出的是00 00 00 02
                            ///输出即为0x02 00 00 00
   printf("%x,%x",ptr1[-1],*ptr2);   ///打印16进制数
   return 0;
}
