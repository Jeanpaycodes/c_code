///指针题目一
#include <stdio.h>
int main()
{
    int a[5] = {1,2,3,4,5};
    int* ptr = (int*)(&a+1);             ///数组的地址应该用数组指针来存储 这里用(int*)强制类型转换
    printf("%d,%d\n",*(a+1),*(ptr-1));   ///a就是首元素的地址 *(a+1)就是第二个元素 &a就是整个数组的地址 &a+1就是跳过整个数组
                                         ///指针指到5的后一位 *(ptr-1)就是5
    return 0;
}
